language: java
jdk:
  - openjdk17

branches:
  only:
    - master

# Travis CI 서버의 Home
cache:
  directories:
    - $HOME/.m2/

script:
  - java --version
  - ./mvnw clean install
  - ./mvnw test

# CI 실행 완료시 메일로 알람
notifications:
  email:
    recipients:
      - yethin99@gmail.com